using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class F_m_Movement : MonoBehaviour
{

   // public Rigidbody2D f_m;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       /* f_m.AddForce(new Vector2(Random.Range(-100, 100) * Time.deltaTime * 2,
          Random.Range(-100, 100) * Time.deltaTime * 2));*/
    }
}
